#include<iostream>
#include<math.h>
#include<string>

using namespace std;
//htisanaughtyboy ht
//abcabc abc

int start = 0;
int cnt_final = 0;
void find (string input, string key)
{
    for(int i = 0, j = 0; i < input.length(); i++)
    {
        if(input[i] == key[j])
        {
            
        }
    }
}

int main()
{
    string input, key;
    cin >> input >> key;

    find(input, key);
    cout << cnt_final << endl;
}